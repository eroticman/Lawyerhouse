$(document).ready(function() {
	
	// enable fileuploader plugin
	$('input[name="pdImg"]').fileuploader({
		// limit:1
	});

	// enable fileuploader plugin
	$('input[name="reImg"]').fileuploader({
		// limit:1
	});
	
});